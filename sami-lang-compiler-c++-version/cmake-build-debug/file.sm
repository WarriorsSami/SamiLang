/@
hello there this is a comment
@/

invk strmfl;

fntn f1_f2 :: ((a, b): int) => (int) {
    return a + b;
}

fntn main :: (argc: int, argv: Vec<str>) => (bare) {
    @@ another comment
    println("%s\n", "Hello, world!");
    println("%d", f1_f2(1, 2));

    let nr = 0x3f3f3f3f;
    var nr_real: float = 20.002;
    let nr2 = 0b0011100;

    &(a, b) ::< strmfl.IO("%d%d", stdin);
    println("%d", f1_f2(a, b));
}